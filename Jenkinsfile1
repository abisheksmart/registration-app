pipeline {
    agent any
    triggers {
        pollSCM('* * * * *') // Optional fallback polling
    }
    stages {
        stage('Build') {
            steps {
                echo 'Building after GitHub push...'
            }
        }
    }
}
